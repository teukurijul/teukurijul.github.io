// assets/main.lazy.js - non-critical animations (particles, mouse-light, tilt, orbit)
(function(){'use strict';
const $=(q,e=document)=>e.querySelector(q),$$=(q,e=document)=>Array.from(e.querySelectorAll(q));
// mesh canvas particles (adaptive)
const canvas=document.getElementById('mesh-canvas');if(canvas){const ctx=canvas.getContext('2d');let W,H,particles=[];const deviceMem=navigator.deviceMemory||4;function resize(){W=canvas.width=canvas.clientWidth;H=canvas.height=canvas.clientHeight;particles=[];const base=Math.max(16,Math.floor((W*H)/90000));const max=deviceMem>4?Math.min(80,base):Math.min(60,base);for(let i=0;i<max;i++)particles.push({x:Math.random()*W,y:Math.random()*H,r:1+Math.random()*3,dx:(Math.random()-0.5)*0.6,dy:(Math.random()-0.5)*0.6})}window.addEventListener('resize',resize,{passive:true});resize();let rafId;function draw(){ctx.clearRect(0,0,W,H);const g=ctx.createLinearGradient(0,0,W,H);g.addColorStop(0,'rgba(7,6,23,0.5)');g.addColorStop(0.5,'rgba(6,12,30,0.35)');g.addColorStop(1,'rgba(4,5,10,0.6)');ctx.fillStyle=g;ctx.fillRect(0,0,W,H);particles.forEach(p=>{p.x+=p.dx;p.y+=p.dy;if(p.x<0||p.x>W)p.dx*=-1;if(p.y<0||p.y>H)p.dy*=-1;ctx.beginPath();ctx.fillStyle='rgba(0,255,163,0.05)';ctx.arc(p.x,p.y,p.r,0,Math.PI*2);ctx.fill()});rafId=requestAnimationFrame(draw)}rafId=requestAnimationFrame(draw)}
// mouse light (throttled via rAF)
const mouseLight=document.getElementById('mouse-light');const mouseEnabled=window.matchMedia('(pointer:fine) and (min-width:720px)').matches;if(mouseLight&&mouseEnabled){let mx=-999,my=-999,need=false;function onMove(e){mx=e.clientX;my=e.clientY;need=true}window.addEventListener('mousemove',onMove,{passive:true});function u(){if(need){mouseLight.style.left=mx+'px';mouseLight.style.top=my+'px';mouseLight.style.opacity=0.12;need=false}requestAnimationFrame(u)}requestAnimationFrame(u);window.addEventListener('mouseleave',()=>{mouseLight.style.opacity=0;}, {passive:true})}
// orbit layout
const orbitItems=$$('.orbit-item');orbitItems.forEach((it,i)=>{const angle=(i/orbitItems.length)*Math.PI*2;const r=120;const x=Math.cos(angle)*r;const y=Math.sin(angle)*r*0.6;it.style.transform=`translate(-50%,-50%) translate3d(${x}px, ${y}px, 0)`});
// tilt cards via pointer + rAF
const cards=$$('.project-card');cards.forEach(card=>{let rect,w,h,px=0,py=0,ticking=false;function update(){const rx=-(py/h)*12,ry=(px/w)*12;card.style.transform=`perspective(900px) rotateX(${rx}deg) rotateY(${ry}deg) translateZ(6px)`;card.style.boxShadow=`${-ry*2}px ${rx*2}px 36px rgba(0,0,0,0.5)`;ticking=false}function onPointer(e){rect=card.getBoundingClientRect();w=rect.width;h=rect.height;px=(e.clientX-rect.left)-w/2;py=(e.clientY-rect.top)-h/2;if(!ticking){ticking=true;requestAnimationFrame(update)}}function reset(){card.style.transform='';card.style.boxShadow=''}card.addEventListener('pointermove',onPointer,{passive:true});card.addEventListener('pointerleave',reset);card.addEventListener('focus',()=>card.classList.add('focus'))});
})();
